---
- hosts: all
  become: yes
  become_user: root

  vars:
    SSH_PORT: 22
    DEFAULT_PACKAGES:
      - ufw
      - fail2ban

  roles:
    - user
    - packages
    - ssh 
    - time
    - config_fail2ban
    - auto_sec_updates