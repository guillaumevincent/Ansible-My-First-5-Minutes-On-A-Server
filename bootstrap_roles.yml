---
- hosts: all
  become: yes
  become_user: pi

  vars:
    SSH_PORT: 22
    DEFAULT_PACKAGES:
      - ufw
      - fail2ban
    TIMEZONE: Europe/Paris

  roles:
    - user
    - packages
    - ssh 
    - time